# Earth Through Deep Time

Static website exploring Earth's geologic history from the Hadean to the Quaternary.

## Local preview

Run a local server from the project root:

```bash
python3 -m http.server 8001
```

Then open `http://localhost:8001`.

## GitHub Pages

This repository includes a GitHub Actions workflow that deploys the root static site to GitHub Pages whenever `main` is updated.
